name: Rookie Models Stage 6 SOS Compact Transfer
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'rookie_models/stage6_sos_compact_transfer.py'
      - '.github/workflows/rookie-models-stage6-sos-compact-transfer.yml'
permissions:
  contents: write
jobs:
  x:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install pandas
      - run: python rookie_models/stage6_sos_compact_transfer.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add rookie_models/results_stage6_sos/prospect_final_school_sos_2014_2023_compact.txt
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Publish compact 2014-2023 SOS transfer"
          git push
# trigger
