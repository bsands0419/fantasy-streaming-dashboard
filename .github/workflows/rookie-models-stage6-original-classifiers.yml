name: Rookie Models Stage 6 Original Classifiers
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'rookie_models/stage6_original_classifiers.py'
      - '.github/workflows/rookie-models-stage6-original-classifiers.yml'
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: rookie_models/requirements_v3.txt
      - run: pip install -r rookie_models/requirements_v3.txt
      - name: Run original classifiers with captured log
        run: |
          mkdir -p rookie_models/results_stage6_newmodels/original_classifiers
          set +e
          python rookie_models/stage6_original_classifiers.py > rookie_models/results_stage6_newmodels/original_classifiers/run.log 2>&1
          code=$?
          echo "$code" > rookie_models/results_stage6_newmodels/original_classifiers/exit_code.txt
          exit 0
      - name: Publish classifier results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add rookie_models/results_stage6_newmodels/original_classifiers/
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Publish Stage 6 original-data classifier search"
          git push
# trigger 2026-08-21T09:02Z
