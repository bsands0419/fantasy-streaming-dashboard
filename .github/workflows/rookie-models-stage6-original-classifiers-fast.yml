name: Rookie Models Stage 6 Original Classifiers Fast
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'rookie_models/stage6_original_classifiers_fast.py'
      - '.github/workflows/rookie-models-stage6-original-classifiers-fast.yml'
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: rookie_models/requirements_v3.txt
      - run: pip install -r rookie_models/requirements_v3.txt
      - run: python rookie_models/stage6_original_classifiers_fast.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add rookie_models/results_stage6_newmodels/original_classifiers_fast/
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Publish Stage 6 fast original-data classifier search"
          git push
# trigger 2026-08-21T09:18Z
